# Beast Back Office - Environment Variables

# OpenClaw Gateway Configuration
# For local development, this defaults to localhost:18789
# For Vercel deployment, you MUST use a tunnel (ngrok) to expose your local gateway
# 
# Setup Instructions:
# 1. Install ngrok: https://ngrok.com/download
# 2. Run: ngrok http 18789
# 3. Copy the HTTPS URL (e.g., https://abc123.ngrok.io)
# 4. Set OPENCLAW_GATEWAY_URL in Vercel dashboard
# 5. Restart your Vercel deployment
#
OPENCLAW_GATEWAY_URL=http://127.0.0.1:18789

# Optional: Request timeout in milliseconds (default: 5000)
OPENCLAW_TIMEOUT=5000

# Webhook Secret for receiving replies from Beast
# Generate a strong secret and set it in both:
# 1. This backoffice (BACKOFFICE_WEBHOOK_SECRET)
# 2. OpenClaw gateway webhook configuration
BACKOFFICE_WEBHOOK_SECRET=your-secret-here

# Vercel Deploy Hook (optional - for programmatic redeploys)
VERCEL_DEPLOY_HOOK=

# Note: Never commit your actual .env.local file to git!
# This .env.example file is safe to commit as a template.
